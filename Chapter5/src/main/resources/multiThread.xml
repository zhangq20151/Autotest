<?xml version="1.0" encoding="UTF-8" ?>
<suite name="thread" parallel="classes" thread-count="3">
    <!--
    classes级别：相同的class tag下的用例在同一个线程中执行，不同的class tag下用例在不同的线程执行
    tests级别：不同的test tag下的用例可以在不同的线程执行；相同的test tag下的用例只能在同一个线程中执行
    methods级别：所有用例（一个class内的多个method）都可以在不同的线程下执行

    thread-count:代表了最大并发线程数

    xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池

    -->

    <test name="demo1">
        <classes name="a">
            <class name="com.course.testng.multiThread.MultiThreadOnXml"/>

        </classes>
    </test>

    <test name="demo2">
        <classes name="b">
            <class name="com.course.testng.multiThread.MultiThreadOnAnnotion"/>
         </classes>

    </test>

</suite>